import { RootState } from '../../store';

export const setDate = (state: RootState) => state.recept.item;

export const setSelectedDate = (state: RootState) => state.recept.selectedDate;

export const setSelectedHour = (state: RootState) => state.recept.selectedDate;

